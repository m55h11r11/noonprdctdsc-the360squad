# This app is BYOK-only. No server-side provider keys needed — every
# generation is authenticated by the key the user pastes into Settings.
# Nothing to configure here unless you want to add something later.
